services:
  - type: web
    name: jd-transcripts-backend
    env: node
    buildCommand: npm install
    startCommand: node backend-server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
    healthCheckPath: /
